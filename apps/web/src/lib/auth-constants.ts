/** Shared auth constants — safe to import from middleware (Edge) and server. */
export const ACCESS_COOKIE = 'gnevo_at';

/** Client-portal session cookie — separate credential from staff `gnevo_at`. */
export const PORTAL_COOKIE = 'gnevo_portal';
